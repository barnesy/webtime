<template>
  <div class="callout">
    <p v-if="this.$slots.label" class="label">
      <slot name="label">Label</slot>
    </p>
    <p v-if="this.$slots.title" class="title">
      <slot name="title"
        >A Callout's Headline is the Main Message</slot>
    </p>
    <p class="support">
      <slot name="support"
        >It might receive some supporting copy, though.
      </slot>
    </p>

    <slot />
  </div>
</template>

<style lang="scss" scoped>
@import "./assets/styles/variables.scss";

.callout {
  width: 100%;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-self: center;
  align-items: center;
  padding: 10rem;
  background-color: $blue;
  color: white;

  a.button {
    margin-top: 3rem;
    align-self: center;
  }
}

.title {
  margin: 0;
  font-family: Bitter-Thin;
  text-align: center;
  font-size: 4.8rem;
  line-height: 1.5;

  @media (max-width: 800px) {
    font-size: 3.6rem;
  }
}

.support {
  color: $white;
  font-size: 26px;
  line-height: 160%;
  width: 50%;
  @media (max-width: 800px) {
    font-size: 1.5rem;
  }
}

a {
  color: $light-blue;
  text-decoration: none;

  @media (max-width: 800px) {
    font-size: 1.5rem;
  }
}
</style>

<script>
export default {
  name: "Callout",
  props: ["url", "labelColor"],
  computed: {
    cssVars() {
      return {
        "--bg-color": this.labelColor,
      };
    },
  },
};
</script>
