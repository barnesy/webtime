<template>
    <figure :class="version">
        <p :class="version" v-if="this.$slots.title" class="title"><slot name="title" /></p>
        <slot />
        <figcaption :class="version" v-if="this.$slots.caption" >
            <slot name="caption" />
        </figcaption>
    </figure>
</template>

<script>

export default {

  name: 'Figure',
  props: {
  	version: {
      type: String,
  	},
  }

}

</script>

<style scoped lang='scss'>
figure {
    margin: 0;
    
    &.dark {
        background-color: var(--dark-grey);
    }
}

img {
    width: 100%; 
}

figcaption { 
    font-size: 1.4rem;
    padding: 2rem;

    &.dark {
        color: white; 
    }
}

.title { 
    font-family: Bitter-Regular, sans-serif;
    font-size: 2.1rem;
    color: var(--blue);

    &.dark {
        color: var(--yellow);
        padding: 2rem;
    }
}
</style>